# 首版验证记录

## 2026-09-24：0.3 布局、状态与交互预览

- 新增框选、组合、锁定、8 像素网格与控件吸附、六向对齐、水平/垂直等间距分布。
- 新增整页独立状态、控件隐藏/禁用、状态说明；项目文件与交接文档保留全部状态及交互配置。
- 新增点击跳转、弹窗开关、返回、切换状态及可点击区域提示；退出预览不修改设计稿。
- 原模型测试与 studio.test.mjs 通过，覆盖组内几何、锁定、吸附、状态独立、预览历史与项目不变性。
- 浏览器测试使用端口 4321 的模块开发版和 references/qa/studio-session 独立数据，未改动 data/project.json 或用户双击版草稿。
- 实际操作验证：组合复制、锁定后删除保护、框选、取消组合/重新组合、垂直等间距分布；复制状态后隐藏控件不影响默认状态；保存后刷新恢复状态。
- 实际预览验证：领取切换到已领取、禁用控件不执行、弹窗打开/关闭、跳转记录页后返回保留状态；退出后编辑状态不变。通过属性面板改为跳转后，预览按新配置执行。
- 查看并核对最终预览布局；测试期间浏览器未记录错误。导出的 .sysplan 包含状态和隐藏属性，HTML 包含全部状态及交互说明，样本已移入 references/qa。
- 独立 index.html 已重新打包并通过内联脚本语法解析。新增功能的浏览器测试针对模块开发版；本轮未重新直接访问 file://，不将开发版测试等同于双击版全流程验收。
- 清洁使用示例保存在 examples/交互与状态_使用示例.sysplan，也可从工作台左下角打开。
- 当前状态是独立整页快照；每次点击执行一个动作，尚无条件分支、连续动作或服务端游戏数据。

## 2026-09-24：改为 index.html 启动

- 查到原启动脚本依赖的 start.ps1 已缺失；根目录新增独立 index.html，启动工具.cmd 改为直接打开这个文件。
- 编辑器、样式、Mermaid 12.0.0 单文件发行包及初始项目均已内联打包，入口约 5.4 MB。
- 双击版使用 IndexedDB 自动保存当前浏览器草稿，项目文件和交接文件改为浏览器下载；保留原服务版数据。
- 打包时对内联应用代码和 Mermaid 进行 JavaScript 语法解析；app.js、storage.js 语法检查及原模型测试通过。
- 内置浏览器的 URL 安全策略禁止 file:// 导航，未尝试绕过。用户在自己的浏览器中双击后明确回复“能打开并看到画布”。
- 这次用户确认覆盖双击启动和画布显示；不能据此宣称文件协议下的所有导出、草稿恢复及 Mermaid 类型都已重新实测。

以下为之前服务版的实际浏览器验证记录，保留作为历史证据。

日期：2026-09-24。环境：Windows，本地服务 + Codex 内置 Chromium 浏览器，桌面布局 1440 × 1000。

实际浏览器操作通过：

- 从组件库添加按钮，修改名称和显示文字，关联条件、操作与结果。
- 控件表自动出现相同规则；切换视图后内容保留。
- PNG 图片通过文件选择器上传，保存项目中保留图片数据。
- 拖动画笔产生笔迹，橡皮擦除整条笔迹，撤回恢复对象。
- 绘制矩形，拖动改变 X 位置，从 104 到 140；撤回恢复 104。
- 思维导图添加子主题、编辑名称、删除主题和撤回恢复分支。
- Mermaid 默认流程图成功渲染；组件位于本地 vendor 目录。
- 页面重新加载后恢复 4 个对象、主题结构及控件规则。
- HTML 交接文档与 .sysplan 文件写入本地；HTML 确认包含图片、规则、SVG 及 Mermaid 内容。
- 导出的 .sysplan 重新导入后，4 个对象及控件规则完整保留。

代码检查：app.js 与 server.cjs 语法检查通过；model.test.mjs 覆盖控件规则导出、导图分支和布局、待确认状态检查。

未声称完成：真实策划项目长期使用、所有 Mermaid 图类型、其他电脑安装与各浏览器兼容性、XMind 原生文件兼容、AI 生成、原生 DOCX 输出。

测试导出的样本保留在 references/qa；工作空间最终打开独立的使用示例。
